<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ZorbaGame.Boxup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ZorbaGame"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Boxup">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid KeyDown="Grid_KeyDown">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid  Grid.Row="0" Grid.Column="0" Background="White">
            <Viewbox x:Name="Viewbox" Stretch="Uniform">
                <Canvas x:Name="game" Width="{x:Bind Width}" Height="{x:Bind Height}"/>
            </Viewbox>
        </Grid>
        <StackPanel x:Name="StatusPanel" Grid.Row="1" Grid.Column="0" Orientation="Vertical">
            <Border x:Name="StatusBorder">
                <!-- Use LiveSetting to enable screen readers to announce the status update. -->
                <TextBlock x:Name="StatusBlock" FontWeight="Bold" AutomationProperties.LiveSetting="Assertive" Foreground="White"/>
            </Border>
        </StackPanel>
    </Grid>
</Window>
