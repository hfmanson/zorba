<Page
    x:Class="TestZorba.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestZorba"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="20">
        <!-- Define 3 rows: 2 fluid rows for TextBoxes, 1 sized to its content for the Button -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- First Multiline TextBox -->
        <TextBox Grid.Row="0"
             Name="xquery"
             Header="XQuery"
             TextWrapping="Wrap"
             AcceptsReturn="True"
             ScrollViewer.VerticalScrollBarVisibility="Auto"/>

        <!-- Second Multiline TextBox -->
        <TextBox Grid.Row="1"
             Name="result"
             Header="Result"
             TextWrapping="Wrap"
             AcceptsReturn="True"
             ScrollViewer.VerticalScrollBarVisibility="Auto"/>

        <!-- Action Button -->
        <Button Grid.Row="2" 
            Content="Submit" 
            HorizontalAlignment="Stretch"
            Click="Button_Click"/>
    </Grid>
</Page>
